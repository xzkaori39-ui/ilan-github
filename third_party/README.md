# 第三方源码镜像

`microsoft-graphrag/` 是 Microsoft GraphRAG 的源码镜像，用于研究、对照官方实现与复现图谱构建流程。其许可证位于 [`microsoft-graphrag/LICENSE`](microsoft-graphrag/LICENSE)。

iLAN 的线上问答链路不直接改写该镜像；项目适配代码位于 `backend/app/graph/` 和 `backend/scripts/`。镜像中不包含任何本项目语料、缓存、模型密钥或 GraphRAG 运行产物。
