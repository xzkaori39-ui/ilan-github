# Loop Engine 独立后台服务：周期性消费反馈执行进化循环
apiVersion: apps/v1
kind: Deployment
metadata:
  name: loop-engine
  namespace: wenshu
  labels:
    app: loop-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loop-engine
  template:
    metadata:
      labels:
        app: loop-engine
    spec:
      containers:
        - name: loop-engine
          image: school-doc-agent:v1.0
          command: ["python", "-m", "scripts.async_worker"]
          env:
            - name: UPLOAD_STORAGE_DIR
              value: "/data/uploads"
            - name: STORAGE_MODE
              value: "mongo"
            - name: EMBEDDING_PROVIDER
              value: "relay"
            - name: EMBEDDING_MODEL
              value: "text-embedding-3-large"
            - name: VECTOR_BACKEND
              value: "mongo"
            - name: LOOP_PHASE
              value: "human_on_loop"
            - name: DEEPSEEK_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wenshu-secrets
                  key: DEEPSEEK_API_KEY
            - name: RELAY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wenshu-secrets
                  key: RELAY_API_KEY
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: wenshu-secrets
                  key: MONGODB_URI
            - name: REDIS_ADDR
              valueFrom:
                secretKeyRef:
                  name: wenshu-secrets
                  key: REDIS_ADDR
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          volumeMounts:
            - name: uploads
              mountPath: /data/uploads
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: wenshu-uploads
