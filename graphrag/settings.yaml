### This config file contains required core defaults that must be set, along with a handful of common optional settings.
### For a full list of available settings, see https://microsoft.github.io/graphrag/config/yaml/

### LLM settings ###
## There are a number of settings to tune the threading and token limits for LLM calls - check the docs.

completion_models:
  default_completion_model:
    model_provider: openai
    model: gpt-5.4
    auth_method: api_key # or azure_managed_identity
    api_key: ${DEEPSEEK_API_KEY}
    api_base: ${DEEPSEEK_BASE_URL}
    retry:
      type: exponential_backoff
      max_retries: 4
      max_delay: 30
    rate_limit:
      type: sliding_window
      requests_per_period: 30
      period_in_seconds: 60
    metrics:
      writer: file
      base_dir: logs

embedding_models:
  default_embedding_model:
    model_provider: openai
    model: text-embedding-v4
    auth_method: api_key
    api_key: ${RELAY_API_KEY}
    api_base: ${RELAY_BASE_URL}
    retry:
      type: exponential_backoff
      max_retries: 4
      max_delay: 30
    rate_limit:
      type: sliding_window
      requests_per_period: 30
      period_in_seconds: 60
    metrics:
      writer: file
      base_dir: logs

# Protect the relay and make a failed run replayable from the file cache.
concurrent_requests: 3

### Document processing settings ###

input:
  type: jsonl
  id_column: id
  title_column: title
  text_column: text

chunking:
  type: tokens
  # Preflight against the active corpus: largest existing i兰 chunk is 771
  # o200k tokens, so 1000 preserves one Mongo chunk per GraphRAG document.
  size: 1000
  overlap: 0
  encoding_model: o200k_base
  prepend_metadata: [title, dept_id, section_title]

### Storage settings ###
## If blob storage is specified in the following four sections,
## connection_string and container_name must be provided

input_storage:
  type: file # [file, blob, cosmosdb]
  base_dir: "input"

output_storage:
  type: file # [file, blob, cosmosdb]
  base_dir: "runs/20260828-gpt54-deepseek/output"

reporting:
  type: file # [file, blob]
  base_dir: "runs/20260828-gpt54-deepseek/logs"

cache:
  type: json # [json, memory, none]
  storage:
    type: file # [file, blob, cosmosdb]
    # Reuse completed GPT-5.4 extraction results from the interrupted run;
    # new misses use the current DEEPSEEK_* completion endpoint.
    base_dir: "cache"
    
vector_store:
  type: lancedb
  db_uri: output/lancedb

### Workflow settings ###

embed_text:
  embedding_model_id: default_embedding_model
  # The configured relay rejects embedding batches larger than 10 inputs.
  batch_size: 10

extract_graph:
  completion_model_id: default_completion_model
  prompt: "prompts/extract_graph.txt"
  entity_types: [department, organization, role, policy, procedure, service, location]
  max_gleanings: 0

summarize_descriptions:
  completion_model_id: default_completion_model
  prompt: "prompts/summarize_descriptions.txt"
  max_length: 500

extract_graph_nlp:
  text_analyzer:
    extractor_type: regex_english # [regex_english, syntactic_parser, cfg]

cluster_graph:
  max_cluster_size: 20

extract_claims:
  enabled: false
  completion_model_id: default_completion_model
  prompt: "prompts/extract_claims.txt"
  description: "Any claims or facts that could be relevant to information discovery."
  max_gleanings: 1

community_reports:
  completion_model_id: default_completion_model
  graph_prompt: "prompts/community_report_graph.txt"
  text_prompt: "prompts/community_report_text.txt"
  max_length: 2000
  max_input_length: 8000

snapshots:
  graphml: false
  embeddings: false

### Query settings ###
## The prompt locations are required here, but each search method has a number of optional knobs that can be tuned.
## See the config docs: https://microsoft.github.io/graphrag/config/yaml/#query

local_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/local_search_system_prompt.txt"

global_search:
  completion_model_id: default_completion_model
  map_prompt: "prompts/global_search_map_system_prompt.txt"
  reduce_prompt: "prompts/global_search_reduce_system_prompt.txt"
  knowledge_prompt: "prompts/global_search_knowledge_system_prompt.txt"

drift_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/drift_search_system_prompt.txt"
  reduce_prompt: "prompts/drift_search_reduce_prompt.txt"

basic_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/basic_search_system_prompt.txt"
