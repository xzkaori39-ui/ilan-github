# Runtime uses the parent project's ignored `.env` through a local symlink.
# It must contain RELAY_API_KEY and RELAY_BASE_URL, the same OpenAI-compatible
# relay credentials used by i兰. Do not add keys to Git.
