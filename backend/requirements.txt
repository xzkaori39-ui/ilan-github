# ---- Web 框架 ----
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.6,<3.0
pydantic-settings>=2.2
python-multipart>=0.0.9

# ---- HTTP 客户端（异步，调用 DeepSeek / 中转站） ----
httpx>=0.27

# ---- 存储 ----
motor>=3.4
redis>=5.0
pymongo>=4.6

# ---- 知识图谱 / GraphRAG ----
# Neo4j 服务独立部署；这两个包安装在 wenshu-py311 Conda 环境中。
neo4j>=5.28,<6
neo4j-graphrag>=1.18,<2

# ---- 检索 ----
# Chroma 为可选：本地向量检索（开发零运维）。生产可切 Milvus。
chromadb>=0.5
pymilvus>=2.4,<3

# ---- 文档解析 ----
# PDF 用 pypdf/pdfplumber；Word 用 python-docx；扫描 PDF 由本地中文 OCR 识别
pypdf>=4.0
pdfplumber>=0.11
python-docx>=1.1
beautifulsoup4>=4.12
rapidocr-onnxruntime>=1.4,<2

# ---- 可选本地向量（无网络/离线 fallback） ----
# sentence-transformers>=2.7

# ---- 数值 / 工具 ----
numpy>=1.26
scikit-learn>=1.4        # DBSCAN 聚类（Skill Miner）
jieba>=0.42              # 中文分词（BM25 / 关键词）

# ---- 可观测性 ----
prometheus-client>=0.20

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
