# i兰 / iLAN Web

Next.js 15 前端，提供聊天、文档管理、知识图谱浏览、评测和管理控制台。

本地开发：

```bash
npm ci
BACKEND_URL=http://localhost:8000 npm run dev
```

生产环境推荐从项目根目录运行 `docker compose up --build -d`。完整的 API、示例文档导入与模型配置说明见根目录 [README](../README.md)。
